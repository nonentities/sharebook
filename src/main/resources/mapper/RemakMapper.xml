<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.swpu.sharebook.mapper.RemakMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.swpu.sharebook.entity.Remak">
        <id column="id" property="id" />
        <result column="orderId" property="orderId" />
        <result column="userId" property="userId" />
        <result column="bId" property="bId" />
        <result column="deliveryId" property="deliveryId" />
        <result column="administrtorId" property="administrtorId" />
        <result column="userToOrder" property="userToOrder" />
        <result column="userToDelivery" property="userToDelivery" />
        <result column="administrtorToUser" property="administrtorToUser" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, orderId, userId, bId, deliveryId, administrtorId, userToOrder, userToDelivery, administrtorToUser
    </sql>

</mapper>
